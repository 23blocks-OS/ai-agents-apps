{
  "name": "ai-agents-apps",
  "version": "1.0.0",
  "private": true,
  "description": "23blocks MCP Apps - Interactive UIs for AI chatbots",
  "scripts": {
    "dev": "pnpm --filter ./apps/* run dev",
    "build": "pnpm --filter ./apps/* run build",
    "build:packages": "pnpm --filter ./packages/* run build",
    "lint": "eslint apps packages",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "eslint": "^8.55.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
